{
  "name": "tokenparty-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @zhouzhengchang/token-party dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "build:publish": "pnpm --filter @zhouzhengchang/token-party build:publish",
    "prepublishOnly": "pnpm run build:publish"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.3.5",
    "socks-proxy-agent": "^10.0.0",
    "undici": "^8.3.0"
  }
}
